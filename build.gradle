task helloWorld {
  doLast {
    println "Hello World, version = " + version
  }
}

// configuration phase
task byeWorld {
  doLast {
    // execution phase
    println "Bye World"
  }
}

apply from: "archiving.gradle"
